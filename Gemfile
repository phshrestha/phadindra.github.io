source "https://rubygems.org"

# Use the github-pages gem so local builds match GitHub Pages' Jekyll
# environment (pins Jekyll, the jekyll-theme-minimal theme, and plugins).
gem "github-pages", group: :jekyll_plugins

# Required for `jekyll serve` on Ruby 3.x (webrick was removed from stdlib).
gem "webrick"
